
/**
 * Module dependencies.
 */

import deliveries from './delivery-reducers';
import { combineReducers } from 'redux';


/**
 * Export `rootReducer`.
 */

export default combineReducers({
  deliveries
});
